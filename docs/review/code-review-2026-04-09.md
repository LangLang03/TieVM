# TieVM 代码审查报告（2026-04-09）

## 审查范围
- `.tbc` FFI 扩展段与序列化兼容性
- `FfiBridge` 动态库调用路径与类型映射
- `.tlbs` 目录/zip 打包与加载
- `ModuleLoader` 符号冲突策略
- `VmError` 结构化错误栈贯通
- `tiebc/tievm` 新命令与输出行为
- 单测与冒烟回归

## 发现清单（按严重级别）

### P0
- 无

### P1
- 无

### P2
- `FfiBridge` 当前“结构体按值”限制为 `size <= 8`，超过阈值返回 `Unsupported`。
  - 状态：已记录为已知限制，未影响当前测试与标准库路径。

### P3
- `.tlbs` zip 目前仅支持 store 模式（无压缩）。
  - 状态：已实现双格式兼容，后续可扩展 deflate。
- 动态调用后端当前上限参数个数为 8。
  - 状态：满足当前标准库与测试，后续可扩展。

## 修复状态
- 全部 P0/P1 风险已关闭（本次无此类问题）。
- P2/P3 项均有明确约束与错误提示，不会静默失败。

## 残余风险
- 跨平台 ABI 深水区（复杂结构体、大量浮点混合参数）仍需进一步专项压测。
- 若第三方库使用特殊对齐/平台扩展调用约定，需增加签名校验与适配策略。

## 签核结论
- 当前实现达到“部署可用的重构首版”验收门槛，可进入下一阶段（ABI 覆盖扩展与性能细化）。
